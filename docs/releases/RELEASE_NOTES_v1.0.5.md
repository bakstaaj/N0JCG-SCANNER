# PI Scanner v1.0.5

- Removes the duplicate Audio/Idle dashboard strip.
- Removes the duplicate mute and volume controls from that strip.
- Keeps low-latency raw PCM playback through the unified audio arbitrator.
- Keeps the center activity badge as the single live source indicator.
- Preserves inline TGID layout and compact typography.
