# PI Scanner v1.0.13

- Prevents the dashboard status-counter row from shrinking to zero height.
- Keeps all six counters visible above the analog control bar.
- Makes the dashboard vertically scrollable on shorter displays.
- Preserves full-width analog controls below the counters.
- Slightly reduces control height only on short screens.
